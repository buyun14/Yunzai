{
  "name": "yunzai-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "TRSS-Yunzai 运维面板（v1 只读）",
  "//": "这是 dashboard/ 自己的依赖表。仓库根目录的 pnpm install 只装内核依赖，前端依赖在这里单独装（见 dashboard/README.md）。构建产物与 node_modules 都不进仓库。",
  "//typescript": "必须显式钉住，不能靠 vue-tsc 的 peer 自动解析：vue-tsc 2.x 会 require('typescript/lib/tsc')，而 TypeScript 7 不再导出这个子路径（实测 ERR_PACKAGE_PATH_NOT_EXPORTED）。不钉住时安装会解析到 7.0.2，typecheck 直接起不来。",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "typecheck": "vue-tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "@mdi/js": "^7.4.47",
    "pinia": "^2.3.1",
    "vue": "^3.5.13",
    "vue-router": "^4.5.0",
    "vuetify": "^3.7.11"
  },
  "devDependencies": {
    "@types/node": "^24.13.6",
    "@vitejs/plugin-vue": "^5.2.4",
    "sass-embedded": "^1.89.0",
    "typescript": "~5.9.3",
    "vite": "^6.4.1",
    "vite-plugin-vuetify": "^2.1.3",
    "vue-tsc": "^2.2.0"
  }
}
